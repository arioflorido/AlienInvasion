name: Docker Image CI

on:
  # workflow_run:
  #   workflows: ["AlienInvasion Pylint"]
  #   types:
  #     - completed
  push:
    # branches:
    #   - main


#     branches: ["main"]
#   pull_request:
#     branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # - name: Set up Docker
      #   uses: docker/setup-docker@v2

      - name: Build the Docker image
        run: docker build . --file Dockerfile --alien_invasion:latest

      # - name: Build Docker Image
      #   run: |
      #     docker build -t docker.pkg.github.com/${{ github.repository_owner }}/${{ github.repository }}/alien_invasion:latest .
      #   working-directory: Dockerfile

      - name: Authenticate to GitHub Packages
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login docker.pkg.github.com -u ${{ github.actor }} --password-stdin

      # - name: Push Docker Image to GitHub Packages
      #   run: |
      #     docker push docker.pkg.github.com/${{ github.repository_owner }}/${{ github.repository }}/alien_invasion:latest
      #   working-directory: path/to/your/dockerfile
# name: Docker Image CI

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# jobs:

#   build:

#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v3
#     - name: Build the Docker image
#       run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
